apiVersion: v1
kind: Pod
metadata:
  name: todos-frontend-pod
  labels:
    todos: frontend
  spec:
    containers:
      - name: frontend
        image: woragis/todos-frontend:latest
        ports:
          - containerPort: 80
